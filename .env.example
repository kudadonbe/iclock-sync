# ---------------------------------------------
# .env.example - Environment configuration template
#
# This file serves as a template for defining environment variables.
# Copy this file to '.env' and fill in your actual settings.
#
# ⚠️ Do NOT commit the actual '.env' file to version control.
#
# Author: Hussain Shareef (@kudadonbe)
# Date: 2025-03-26
# ---------------------------------------------

# Firebase Admin SDK key path (JSON file)
FIREBASE_KEY=config/firebase-key.json

# iClock Device IP addresses (comma-separated if multiple)
DEVICE_IPS=172.XX.XX.XX,172.XX.XX.XX

# iClock Device Names (comma-separated, match order with DEVICE_IPS)
DEVICE_NAMES=Office Device, Gate 1 Device

# Port for iClock device connection (default: 4370)
DEVICE_PORT=4370

# Timeout duration for device connection in seconds (default: 5)
DEVICE_TIMEOUT=5
