# ---------------------------------------------
# .gitattributes - Git file attribute definitions
#
# Controls how files are treated in Git, including line endings and text encoding.
#
# Ensures consistent behavior across platforms (Windows, macOS, Linux).
#
# Author: Hussain Shareef (@kudadonbe)
# Date: 2025-03-26
# ---------------------------------------------

# Default behavior for all text files
* text=auto

# Explicitly handle dotenv (.env) files as plain text
.env text
.env.example text

# Explicitly set JSON files as UTF-8 encoded text
*.json text

# Force shell and Python scripts to always use LF (Unix-style) line endings
*.sh text eol=lf
*.py text eol=lf
